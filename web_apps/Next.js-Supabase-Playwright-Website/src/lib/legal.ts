/**
 * Version of the privacy policy currently in effect. Bump this (and the
 * "Effective" date in src/app/privacy/page.tsx) whenever the policy text
 * changes materially — every recorded consent stores the version a user
 * agreed to, so old records stay tied to the copy they actually saw.
 */
export const PRIVACY_POLICY_VERSION = "2026-08-08";
