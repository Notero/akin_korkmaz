#include "websocketclient.h"

// Minimal stub constructor keeps the type instantiable while signalling that
// the real WebSocket wiring still needs to be implemented.
websocketclient::websocketclient() {}
